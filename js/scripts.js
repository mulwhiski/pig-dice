function player(name, turns, score) {
  this.playerName=name;
  this.turnStatus=turns;
  this.totalScore=score;
}

$(document).ready(function() {
  $("button#roll").click(function(event) {
    event.preventDefault()
}
$("roll").click(function() {
  math.floor(math.random()*13)
  if(math<2) {
    alert(dice rolled 1)
  });  
});
